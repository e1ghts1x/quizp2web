function iniciar(){

    var conv = prompt("Quer jogar? Digite Sim ou Não")
        
    if(conv === 'Sim' || conv === 'sim'){
        alert("Bem vindo ao jogo!");
    }else if(conv ===  'Não'|| conv === 'não' || conv === 'nao'){
        alert("Batatinha frita 1,2,3...");
    }else{ 
        alert("Resposta inválida!");
    }}
    